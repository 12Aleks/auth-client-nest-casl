const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'i.kinja-img.com'
            },
        ],
    }
}

module.exports = nextConfig