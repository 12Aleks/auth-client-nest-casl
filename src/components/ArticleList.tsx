import React from 'react';
import {wrapper} from "../store/store";

const ArticleList = () => {
    return (
        <div>

            
        </div>
    );
};

export default ArticleList;

